sudo apt-get install libxml2-dev libxslt-dev # lxml

virtualenv env
. env/bin/activate
pip install -r requirements.txt

bash install/install.sh # live patch, risky-risky

# install scapy (to virtualenv) if you want tcp scanning capabilities
# to enable scapy become root and activate the virtualenv
